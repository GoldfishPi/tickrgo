import React, {FC} from 'react';
import {Text} from 'react-native';

interface AuthScreensProps {}

const AuthScreens: FC<AuthScreensProps> = ({}) => {
    return <Text>AuthScreens works!</Text>;
};

export default AuthScreens;
