import React, { FC } from 'react';
import {Text} from 'react-native-paper';
import { View } from 'react-native';

interface FlowScreenProps {
}

const FlowScreen:FC<FlowScreenProps> = ({}) => {
    return (
        <View>
            <Text>Flow Screen</Text>
        </View>
    );
};

export default FlowScreen;
